<h1>All students:</h1>


<ul>
<div>
  <% @students.each do |student| %>
    <li><div><%= link_to student.first_name + " " + student.last_name, student_path(student.id) %></div>
  <% end %>
</div>
</ul>